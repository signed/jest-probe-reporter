{
  "name": "@signed/jest-alphabetical-sequencer",
  "version": "0.1.1",
  "description": "Test sequencer to run tests in a defined deterministic order",
  "main": "dist/test-sequencer.js",
  "repository": "signed/jest-probe-reporter",
  "license": "MIT",
  "files": [
    "README.md",
    "dist",
    "src"
  ],
  "scripts": {
    "build": "rimraf dist && tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "yarn build"
  },
  "keywords": [
    "jest",
    "sequencer"
  ],
  "peerDependencies": {
    "jest": ">= 24.x"
  },
  "devDependencies": {
    "@jest/types": "28.1.0",
    "jest": "28.1.0",
    "rimraf": "3.0.2",
    "typescript": "4.7.2"
  }
}
